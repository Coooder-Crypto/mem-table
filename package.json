{
  "name": "mem-table",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first structured ledger runtime for long-running agents.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm build && pnpm -r test",
    "typecheck": "pnpm build && pnpm -r typecheck"
  },
  "keywords": [
    "agent",
    "memory",
    "ledger",
    "mcp"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.27.0"
}
