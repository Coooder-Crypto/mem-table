{
  "name": "@memtable/server",
  "version": "0.0.0",
  "type": "module",
  "description": "HTTP observer and MCP server for MemTable.",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test test/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@memtable/core": "workspace:*"
  },
  "files": [
    "dist"
  ]
}

